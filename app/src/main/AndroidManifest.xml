<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="taher.restaurant">

    <application
        android:name=".base.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.addupdate.AddExtraActivity"
            android:exported="false" />
        <activity
            android:name=".ui.addupdate.AddAndUpdateActivity"
            android:exported="false" />
        <activity
            android:name=".ui.ConfirmOrderActivity"
            android:exported="true" />
        <activity
            android:name=".ui.NewOrderActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.printer.PrinterConnectActivity" />
        <activity android:name=".ui.addupdate.AddItemActivity" />
        <activity android:name=".ui.addupdate.AddCategoryActivity" />
    </application>

</manifest>